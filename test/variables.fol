fn main() {
  let my_variable = 42; //: iscore
  my_variable += 1;
  debug("my_variable = {my_variable}");
  
  let my_creeper = summon(:creeper, @s.pos(), nbt.Fuse = my_variable); //: entity
  at my_creeper {
    my_creeper.tp(~ ~5 ~);
  };
}

